# URL of the deployed Google Apps Script Web App (the .../exec URL from
# Deploy > Manage deployments in the Apps Script editor).
# Copy this file to .env and fill in the real value — never commit .env.
VITE_APPS_SCRIPT_URL=
